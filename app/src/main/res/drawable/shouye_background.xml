<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 填充 -->
    <solid android:color="@color/mianColor" />
    <!-- 圆角 -->
    <corners android:radius="10dp" />

    <!-- 描边 -->
    <stroke
        android:width="1dp"
        android:color="@android:color/black" />

</shape>